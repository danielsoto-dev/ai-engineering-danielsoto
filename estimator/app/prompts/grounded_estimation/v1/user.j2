<retrieved_context>
{{ context }}
</retrieved_context>

<project_to_estimate>
{{ description }}
</project_to_estimate>

Estimate the project above using only the retrieved context. Attribute every line to the `chunk_id`s that support it, and mark as ungrounded any component the context cannot back.
